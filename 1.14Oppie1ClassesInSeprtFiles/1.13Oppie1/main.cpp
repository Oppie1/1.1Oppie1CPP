#include <iostream>
#include "AdamClass.h"
using namespace std;



int main() {

	AdamClass adamObject;//This is how we call an object (adamObject) or the Adam Class.
	//AdamClass first so the object (like a blank form, empty box, frame of house, etc) knows
	//where to get its information from (could be parts of form to fill out, things to put in box
	//rooms to add to frame of house). blueprint for archetecture of that object.

	//Here we have a constructor that performs an action automatcially.  Perhaps this just means
	//so of form is filled in automatically and just requires signature, a house that has 
	//four bedrooms by default, etc.

	return 0;
}