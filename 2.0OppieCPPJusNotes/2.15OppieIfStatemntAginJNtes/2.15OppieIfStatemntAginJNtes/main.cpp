#include <iostream>
using namespace std;



int main()
{
	//Create an integer variable x and set its value to 11. Then create another integer variable y and set it to 43
	//CODE:


	//Here we set a variable (here x) to a value using the "=" operator
	// //Here we set another variable (y) to a value. The value must match the data type
	//'int' data type-> (int = integer). *i.e. Here we declare and initialize another integer variable*

	//Create an if statement where x is less than or equal to y and then show message out to screen if condition is true.
	//CODE:
	

	//Create cout statement that will say that if the condition were false, and y were less than x, then the if code block
	//would be skipped but this cout statement would still print out to screen as next step in code. 
	//CODE:


	//Please go ahead an change 'x' and 'y' to see the changes to the output
	//Feel free to change the control condition (here <=) to see change in output as well.
	//could use <,>,==(equals),!=(does not equal)
}

//Bonus:
//Programmers often stack 'if' statements to show multiple output parameters.
//This stacked statements are referred to as blocks of code. They may test different
//things and produce different output. 
//Try adding a new 'if' block to see what happens!

//Remember that main(){     code blocks, variables and outputs - like cout statements - here
//is its own function.