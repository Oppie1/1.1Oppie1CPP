#include <iostream>
using namespace std;


//This tutorial explains how we use arrays and shows how to pick a number from the array

int main() {

	//As part of initializing array along with data type we need to let computer know how many numbers (called elements when talking
	//about an array) [total elements] we are referring to. Here we have a list of 4 numbers.
	//For computers arrays start at index 0. And so here the array is 0 - 4 numbers (5 numbers in total) 
	// so 61 is at 0 index, 71 is at 1 index, 3 is at 2, 43 is at 3 is at the third position in the array

	//Again, these are Array initializer list "elements"
	//Create/Declare an integer array "adamNumbers" and initialize the array with 4 numbers(elements) -> 61, 71, 3, 43.
	//CODE:
	

	//Below are examples on how to pull numbers out of array and show them to the screen. Here we do that by printing (cout)
	//the index location of given values.

	
	
	//At index location 3 we will have the number 43.
	//Create a cout statement to the screen stating that "At the index location 3 the number is: "  
	//CODE:
	

	//Create an cout statement that says: "At index location 1 we will have the number: "
	//CODE:
	

	//At index location 2 we will have the number:
	//CODE:


	//At index location 0 we will have the number:
	//CODE:

}

