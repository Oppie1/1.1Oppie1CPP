#include <iostream>
using namespace std;


//This tutorial demonstrates taking an array of 9 elements and displaying the elements from that
//array to the screen. Here we take th array[x] and assign the elements to the array to values
//within the for loop. So each element in the adamArray to 99 (adamArray[99]) which makes each
//element in the array to 99 (0-8) again computers count from 0. So 0-8 = 9 elements


int main()
{

	//This creates 9 indices in the int array.
	//Create/Declare an integer array named adamArray with 9 elements.
	//CODE:
	

	//This creates the heading for our array
	//Create a cout statement that provides a header "Element - Value" at the top of the array as a heading.
	//CODE
	

	//The following demonstrates we can use the variable "x" to produce the corresponding values in the 
	//for loop 0-8 - and the value of "x" array at same time. Computer knows we're talking about two different 
	//values for x.

	//Create a for loop that that declares an integer x and initializes that variable to 0, then write a control statement
	//where x is less than or equal to 8 and the iterate x by 1. This will loop through 9 index locations.
	//CODE:
	

		//So on first iteration initialize in the first section (of 3) -> for(initialize section, test, increment)   
		//x = 0, 0<=8? True, execute code block (set [x] to 99 -> cout number we are dealing with 0-9 with corresponding
		//value here 99 to screen (the cout statement below executes 9 times) 0 -------- 99, 1 ------- 99, 2 ------- 99, ect.
		//Once x = 9 the loop stops and we go back into main function.

		//Create an expression that takes the adamArray with index location x (current position) and sets it equal to 99;
		//CODE:
		

		//Now see that information x (given location of iteration) with the element located at that i. The formatting should match header.
	   //CODE:
	

	//Create a cout congratulation statement that displays to the screen notifying the program has fully executed the loop/codeblock 
	//And that back in main part of program/next step.
	//CODE:
	

	}