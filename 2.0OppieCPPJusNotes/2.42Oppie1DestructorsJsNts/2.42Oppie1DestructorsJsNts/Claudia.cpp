#include <iostream>
#include "Claudia.h"
using namespace std;


//Here we have our function our function definition from the prototype in our header file (just horizonal version of header).  
//e.g. Class Claudia{ -> Here we don't need to say "class" it is implied. Just Claudia
// public:
// Claudia();
// When we create and object in main this is the flow: 
// main.cpp Claudia(class) cv(object) ->.h Claudia(class) Claudia()(function) -> Claudia(class)::Claudia()function

//Define the Claudia constructor of the Claudia class with cout statement "this is the constructor runs automatically 
//when object is created" and another cout statement that "it runs from .cpp file"
//CODE:


//This is the same concept as above. The only difference is that we use the "~" to signify to use a destructor.

//Define the Claudia destructor with the message "This is the destructor and runs automatically as main ends when objects are 
//being destroyed and another cout statement that it runs from the .cpp file.
//CODE:
